package controller;

import javax.management.ObjectInstance;

import database.ConnectorService;

public class ReportFactory {

	public Report criarReport(ConnectorService connector) {
		
		if()
		return new Report(connector);
	}
}
